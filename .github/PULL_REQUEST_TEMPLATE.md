## What & why

<!-- What changes for a user of the plugins, and why. Link the issue if there is one. -->

## Checklist

- [ ] Changeset added (`pnpm changeset`) — or this is a chore-only change
- [ ] Package `README.md` updated if options/behaviour changed
- [ ] e2e coverage for plugin wiring / API / config changes; no real network calls
- [ ] `pnpm build && pnpm typecheck && pnpm lint && pnpm test` is green locally
